IXC_MODE=real
IXC_HOST=ixc.connectfibra.net
IXC_USER=100
IXC_TOKEN=20e92d6321793317e3d43dab04feba4d46664a78512e9b030e2186e15e8803ed
IXC_VERIFY_TLS=true

# --- Banco ---
POSTGRES_DB=softhub
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# --- Core API ---
CORE_API_WORKERS=2
DATABASE_URL=postgresql+psycopg://postgres:postgres@postgres:5432/softhub
REDIS_URL=redis://redis:6379/0
CORS_ALLOW_ORIGINS=
CORS_ALLOW_CREDENTIALS=false
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*

# --- Frontend ---
VITE_API_BASE=/api
VITE_FEATURE_ADMIN=false

# --- Opcional: proteção basic auth no Nginx ---
# 1) Gere o arquivo .htpasswd:
#    docker run --rm --entrypoint htpasswd httpd:2 -Bbn admin SENHA_FORTE > .htpasswd
# 2) Monte em /etc/nginx/.htpasswd no serviço web no compose de produção.
